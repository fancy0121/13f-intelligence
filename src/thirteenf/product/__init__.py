"""Minimum Institutional Evidence Product (v0.4).

Deterministic product/query layer over the FACT DB and committed local
resolution/semantic artifacts. EVIDENCE ONLY: reads local landed facts and
produces facts and derived facts only, with no recommendations or rankings.
"""

__version__ = "0.4.0"
